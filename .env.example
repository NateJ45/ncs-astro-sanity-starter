# Copy this file to `.env` and fill in real values for local dev.
# In production, set these in Cloudflare → Workers & Pages → your project → Settings → Variables.
#
# Leave PUBLIC_SANITY_PROJECT_ID unset (or empty) to build with empty content;
# pages will render their empty-state fallbacks. No Sanity project is required
# to clone and build this starter.

# ── Sanity ────────────────────────────────────────────────────────────────────
# Create a project at sanity.io/manage, then paste its project ID here.
PUBLIC_SANITY_PROJECT_ID=
PUBLIC_SANITY_DATASET=production
PUBLIC_SANITY_API_VERSION=2026-05-01

# Read token — bypasses Sanity's anonymous API filter so the full dataset is
# readable at build time. Create at sanity.io/manage → your project → API → Tokens
# with Viewer (read-only) permission. Mark as Secret in Cloudflare.
SANITY_API_READ_TOKEN=

# Write token — only needed for seed/import scripts (e.g. npm run seed:*).
# Create with Editor permission. Never expose as PUBLIC_.
SANITY_API_WRITE_TOKEN=

# ── Sanity Studio ─────────────────────────────────────────────────────────────
# The Studio lives in THIS package and is served at /studio (there is no nested
# studio/ directory since 2026-08-28). Bundled by Astro it reads the PUBLIC_
# vars above; these SANITY_STUDIO_ variables are what the `sanity` CLI reads
# (typegen, dataset, cors), so set them if you drive the CLI.
SANITY_STUDIO_PROJECT_ID=
SANITY_STUDIO_DATASET=production
# Your deployed Workers URL, used for the "view it live" links in the Studio.
SANITY_STUDIO_PREVIEW_URL=

# ── Live draft preview (/preview/**) ──────────────────────────────────────────
# Its Sanity token is a WORKER RUNTIME secret, not a build-time variable, so it
# does NOT belong in this file. See `.dev.vars.example` for SANITY_TOKEN and for
# the one-time `sanity cors add` step the embedded Studio needs.

# ── Contact form ──────────────────────────────────────────────────────────────
# Without this key the contact form falls back to a no-op + inline notice.
# Get a free key at web3forms.com.
PUBLIC_WEB3FORMS_KEY=

# ── Scheduling ────────────────────────────────────────────────────────────────
# Your Calendly public URL (e.g. https://calendly.com/yourusername).
PUBLIC_CALENDLY_URL=

# ── Analytics ─────────────────────────────────────────────────────────────────
# Both are optional and independent. An empty value omits that tag entirely and
# the site works normally, so a fresh clone and local dev need neither.
#
# IF THIS SITE IS REPLACING AN EXISTING ONE, fill in the measurement id BEFORE
# the domain moves. A rebuild inherits the domain and the content but not the
# analytics tag, and the loss is invisible for weeks because a 30 day total
# decays one day at a time instead of dropping. See PORTS.md card 54.

# Cloudflare Web Analytics: a hex token from the Cloudflare dashboard under
# Analytics and Logs, Web Analytics, Add a site. Cookieless, no banner needed.
PUBLIC_CF_ANALYTICS_TOKEN=

# Google Analytics 4: the WEB DATA STREAM Measurement ID, which looks like
# G-XXXXXXXXXX. It is NOT the numeric property id (a 9 digit number such as
# 552910285) that the GA4 admin screen shows beside the property name. Find it
# under Admin, Data streams, pick the web stream, top right of the panel.
# Anything that does not match G-[A-Z0-9]+ is ignored and no tag renders.
PUBLIC_GA_ID=

# ── Newsletter / ESP ──────────────────────────────────────────────────────────
# Optional build-time override for the newsletter form action URL.
# Prefer setting formActionUrl in Sanity Studio (siteSettings → Newsletter).
PUBLIC_NEWSLETTER_FORM_ACTION=
